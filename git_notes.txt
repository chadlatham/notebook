# configure git for first use with username and email
git config --global user.name 'chadlatham'
git config --global user.email 'chadlatham33@gmail.com'

# initialize a git repository
# create a .git folder in the current working directory
git init

# add files to the staging area
git add <filename> <...>

# to commit files from the staging area
git commit -m 'Message text'

# gitHub has its own copy of the .git repository

# gitHub has its own branch called master that gets synchronized with the local master branch when when git push is called.

# Further notes will be incorporated from Word....
